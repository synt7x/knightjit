#include <cstddef>
#include <cstdint>
#include <iostream>

#include "shared.hpp"

using pool = uint64_t*;

constexpr uint8_t ADD_INT[100] = {
    // <GUARD>:
    0x48, 0x89, 0xca, // mov rdx, rcx
    0x48, 0x83, 0xe2, 0x07, // and rdx, 0x7
    0x74, 0x06, // jz <SKIP>
    0xe9, 0x00, 0x00, 0x00, 0x00, // jmp <STUB>
    
    // <SKIP>:
    0x49, 0xff, 0x06, // inc qword [trace_slot_1]
    0x48, 0x01, 0xcb, // add rbx, rcx

    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x2E, 0x0F, 0x1F, 0x84, 0x00, 0x00, 0x00, 0x00, 0x00, // nop DWORD PTR [rax+rax*1+0x0]
    0x66, 0x0f, 0x1f, 0x44, 0x00, 0x00, // nop
    0x0f, 0x1f, 0x04, 0x00,
    // <END>:
};

constexpr uint8_t ADD_STR[100] = {
     // <GUARD>:
    0x48, 0x89, 0xca, // mov rdx, rcx
    0x48, 0x83, 0xe2, 0x07, // and rdx, 0x7
    0x48, 0x83, 0xfa, 0x03, // cmp rdx, 3
    0x74, 0x06, // jz <SKIP>
    0xe9, 0x00, 0x00, 0x00, 0x00, // jmp <STUB>
    
    // <SKIP>:
    0x49, 0xff, 0x46, 0x08, // inc qword [trace_slot_2]

    0x48, 0x83, 0xe1, 0xf8, // and rcx, 0xfffffffffffffff8
    0x4c, 0x8b, 0x01, // mov r8, QWORD PTR [rcx]
    0x49, 0xc7, 0xc1, 0x00, 0x00, 0x00, 0x00, // mov r9, 0
    0x49, 0xc7, 0xc2, 0x00, 0x00, 0x00, 0x00, // mov r10, 0

    // <TOP>:
    0x4d, 0x39, 0xc1, // cmp r9, r8
    0x7d, 0x22, // jge <END>
    0x4e, 0x0f, 0xb6, 0x5c, 0x09, 0x08, // movzx r11, BYTE PTR [rcx+r9*1+0x8]
    0x49, 0x83, 0xfb, 0x30, // cmp r11, '0'
    0x7c, 0x16, // jl <END>
    0x49, 0x83, 0xfb, 0x39, // cmp    r11,'9'
    0x7f, 0x10, // jg <END>
    0x49, 0x83, 0xeb, 0x30, // sub r11, 0x30
    0x4d, 0x6b, 0xd2, 0x0a, // imul r10, r10, 0xa
    0x4d, 0x01, 0xda, // add r10, r11
    0x49, 0xff, 0xc1, // inc r9
    0xeb, 0xd9, // jmp <TOP>

    0x49, 0xc1, 0xe2, 0x03,// shl r10, 0x3
    0x4c, 0x89, 0xd1, // mov rcx, r10

    0x48, 0x01, 0xcb, // add rbx, rcx
    0x0F, 0x1F, 0x80, 0x00, 0x00, 0x00, 0x00, // nop
    0x90, // nop
};

constexpr uint8_t STUB1[256] = {
    // I'm tired of writing polymorphic assembly by hand,
    // so we're going to assume its a string

    // Imagine we have type checking logic, and we handle ALL other types
    // and shunt them into integers in rcx
    0x49, 0xff, 0x46, 0x08, // inc qword [trace_slot_2]

    0x48, 0x83, 0xe1, 0xf8, // and rcx, 0xfffffffffffffff8
    0x4c, 0x8b, 0x01, // mov r8, QWORD PTR [rcx]
    0x49, 0xc7, 0xc1, 0x00, 0x00, 0x00, 0x00, // mov r9, 0
    0x49, 0xc7, 0xc2, 0x00, 0x00, 0x00, 0x00, // mov r10, 0

    // <TOP>:
    0x4d, 0x39, 0xc1, // cmp r9, r8
    0x7d, 0x22, // jge <END>
    0x4e, 0x0f, 0xb6, 0x5c, 0x09, 0x08, // movzx r11, BYTE PTR [rcx+r9*1+0x8]
    0x49, 0x83, 0xfb, 0x30, // cmp r11, '0'
    0x7c, 0x16, // jl <END>
    0x49, 0x83, 0xfb, 0x39, // cmp    r11,'9'
    0x7f, 0x10, // jg <END>
    0x49, 0x83, 0xeb, 0x30, // sub r11, 0x30
    0x4d, 0x6b, 0xd2, 0x0a, // imul r10, r10, 0xa
    0x4d, 0x01, 0xda, // add r10, r11
    0x49, 0xff, 0xc1, // inc r9
    0xeb, 0xd9, // jmp <TOP>

    0x49, 0xc1, 0xe2, 0x03,// shl r10, 0x3
    0x4c, 0x89, 0xd1, // mov rcx, r10

    0x48, 0x01, 0xcb, // add rbx, rcx
    0xe9, 0x00, 0x00, 0x00, 0x00, // jmp <STUB>
};

constexpr uint8_t STUB2[256] = {
    // See prior, assume its an integer

    // Imagine we have type checking logic, and we handle ALL other types
    // and shunt them into integers in rcx
    0x49, 0xff, 0x06, // inc qword [trace_slot_1]
    0x48, 0x01, 0xcb, // add rbx, rcx
    0xe9, 0x00, 0x00, 0x00, 0x00, // jmp <STUB>
};

void generate_thunk(const uint8_t thunk[], size_t thunk_size, size_t jmp_offset, uint8_t* stub, uint8_t* target, uint8_t buffer[]) {
    memcpy(buffer, thunk, thunk_size);

    const uint8_t* jmp = target + jmp_offset;
    int32_t rel32 = static_cast<int32_t>(stub - (jmp + 5));

    memcpy(&buffer[jmp_offset + 1], &rel32, sizeof(int32_t));
}

void tracer(pool trace_pool, void(*program)()) {
    uint8_t* code = reinterpret_cast<uint8_t*>(program);
    uint8_t patch = 0;

    uint8_t* target_address = code + 52;
    uint8_t* stub = code + 184;

    std::cout << "[Tracer] Stub pool located " << (stub - target_address) << " bytes from the target address." << std::endl;
    std::cout << "[Tracer] Tracing thread started." << std::endl;

    while (true) {
        uint64_t slot1 = trace_pool[0];
        uint64_t slot2 = trace_pool[1];

        if (patch == 0 && slot1 > 10000) {
            std::cout << "[Tracer] Integer phase solidification detected at count: " << slot1 << std::endl;

            uint8_t thunk[100];
            generate_thunk(ADD_INT, sizeof(ADD_INT), 9, stub, target_address, thunk);

            uint8_t stub1[256];
            generate_thunk(STUB1, sizeof(STUB1), 6, target_address, stub, stub1);

            flush(code);

            patch = 1;
            std::cout << "[Tracer] Specialized integer trace patched successfully." << std::endl;
        } else if (patch == 1 && slot2 > 10000) {
            std::cout << "[Tracer] String phase solidification detected at count: " << slot2 << std::endl;

            uint8_t thunk[100];
            generate_thunk(ADD_STR, sizeof(ADD_STR), 9, stub, target_address, thunk);

            uint8_t stub2[256];
            generate_thunk(STUB1, sizeof(STUB2), 43, target_address, stub, stub2);

            flush(code);

            patch = 1;
            std::cout << "[Tracer] Specialized string trace patched successfully." << std::endl;
            break;
        }
    }
}